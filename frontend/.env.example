# Leave unset for local dev (Vite proxies /api to localhost:8000, see vite.config.ts).
# In Vercel, set this to your deployed backend's origin, e.g.:
VITE_API_BASE_URL=https://beacon-backend.onrender.com
